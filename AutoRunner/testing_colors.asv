files = dir('images/*.jpg')';
count = 1;
for file = files,
	
	
	img = imread(['images/' file.name]);
	figure; imshow(img);
    [firstColor, secondColor] = getColorByHSV(img);
    fprintf('\nFigure %d\n', count);
    count = count + 1;
    disp(firstColor);
    disp(secondColor);
	
	
end