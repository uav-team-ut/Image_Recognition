#!/usr/bin/env python

import json
import sys

import process_image

# Get base64 image from arguments
image = sys.argv[1]

# Get targets from image
targets = process_image.get_targets(image)

# Print out targets in a JSON string
print(json.dumps(targets))
